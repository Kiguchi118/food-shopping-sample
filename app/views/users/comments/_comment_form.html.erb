<%= form_with(model: [@item, @comment], url: item_comments_path(@item), remote: true) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <%= f.text_area :content, placeholder: "レビューを投稿..." %>
    <%= f.submit "投稿", class: "comment-post" %>
<% end %>
